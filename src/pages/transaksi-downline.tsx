import { title } from "@/components/primitives";

export default function TransaksiDownlinePage() {
  return (
    <div>
      <h1 className={title({ size: "xs" })}>Transaksi Downline</h1>
    </div>
  );
}
